#  Matching CVs based on EDISON Data Science Competencies (CF-DS): Front end

Run `npm install` to install dependencies, then run `ng serve` to start a server.
Visit localhost:4200 to view front end.
